// there is an integer array nums sorted in ascending order with distinct value prior to being passed to your function nums is possibly rotated at an unknown pivot index K where K is greater than equal to one and less than NUM's length such that the resultant array is NUM K NUM NUM K1 NUM K2 and so on up to nums and one N zero N 1 up to NK10 is the zero index For example[0,1,2,4,5,6,7] might be rotated at a pivot index three and become[4,5,6,7,0,1,2]. given the array norms after the possible rotation and integer target return the index of the target if it is in the nums array or return one if it is not in the nums array you must write your algorithm with of runtime complexity.
package QuesPracticeArray;

public class Array6 {
    public static void main(String[] args) {
        int []arr = {4,5,6,7,0,1,2};

    }
}
